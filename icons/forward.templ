package icons

// Renders the Lucide icon forward.
templ Forward(attrs ...templ.Attributes) {
	<svg
		{ at(attrs)... }
		class={ cn("lucide lucide-forward", attrs) }
	>
		<polyline points="15 17 20 12 15 7"></polyline>
		<path d="M4 18v-2a4 4 0 0 1 4-4h12"></path>
		{ children... }
	</svg>
}
