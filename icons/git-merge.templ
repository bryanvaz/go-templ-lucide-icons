package icons

// Renders the Lucide icon git-merge.
templ GitMerge(attrs ...templ.Attributes) {
	<svg
		{ at(attrs)... }
		class={ cn("lucide lucide-git-merge", attrs) }
	>
		<circle cx="18" cy="18" r="3"></circle>
		<circle cx="6" cy="6" r="3"></circle>
		<path d="M6 21V9a9 9 0 0 0 9 9"></path>
		{ children... }
	</svg>
}
