package icons

// Renders the Lucide icon message-circle.
templ MessageCircle(attrs ...templ.Attributes) {
	<svg
		{ at(attrs)... }
		class={ cn("lucide lucide-message-circle", attrs) }
	>
		<path d="M7.9 20A9 9 0 1 0 4 16.1L2 22Z"></path>
		{ children... }
	</svg>
}
