package icons

// Renders the Lucide icon brackets.
templ Brackets(attrs ...templ.Attributes) {
	<svg
		{ at(attrs)... }
		class={ cn("lucide lucide-brackets", attrs) }
	>
		<path d="M16 3h3v18h-3"></path>
		<path d="M8 21H5V3h3"></path>
		{ children... }
	</svg>
}
