package icons

// Renders the Lucide icon bluetooth.
templ Bluetooth(attrs ...templ.Attributes) {
	<svg
		{ at(attrs)... }
		class={ cn("lucide lucide-bluetooth", attrs) }
	>
		<path d="m7 7 10 10-5 5V2l5 5L7 17"></path>
		{ children... }
	</svg>
}
