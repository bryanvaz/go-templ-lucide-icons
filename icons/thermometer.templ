package icons

// Renders the Lucide icon thermometer.
templ Thermometer(attrs ...templ.Attributes) {
	<svg
		{ at(attrs)... }
		class={ cn("lucide lucide-thermometer", attrs) }
	>
		<path d="M14 4v10.54a4 4 0 1 1-4 0V4a2 2 0 0 1 4 0Z"></path>
		{ children... }
	</svg>
}
